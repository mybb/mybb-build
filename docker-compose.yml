version: "3.2"

services:
    phing-service:
        image: mybb-build
        build:
            context: .
        volumes:
            - ./input:/home/user/input/:ro
            - ./output:/home/user/output/
